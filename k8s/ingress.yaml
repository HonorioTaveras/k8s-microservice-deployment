apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: metrics-api
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
  - host: metrics.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: metrics-api
            port:
              number: 80
